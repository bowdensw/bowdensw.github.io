# Web3Forms access key for the contact form (https://web3forms.com — free tier).
# Without it the form renders a notice instead of silently dropping messages.
# Public by design: this is a client-side POST from a static export.
NEXT_PUBLIC_WEB3FORMS_KEY=
